name: basic-demo
description:
tags:
initialize:
  plugins:
    datadog-importer: 
      path: 'datadog-importer'
      method: DatadogImporter
      global-config:
        instance-id-tag: kube_node
        location-tag: region
        instance-type-tag: instance-type
        cpu-utilization-metric-name: system.cpu.user
tree:
  children:
    child-0:
      pipeline:
        - datadog-importer
      config:
        datadog-importer:
          time-step: 10
      inputs:
        - timestamp: 2024-03-30T00:00
          duration: 60
          instance-id: ip-10-163-212-35.us-east-2.compute.internal
